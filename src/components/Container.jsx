var React = require('react');
var Search = require('./Search.jsx');

// dummy data
var videoTitle = "generationals - alix";


module.exports = React.createClass({
	render: function() {
		return (
			<Search />
		);
	}
});