module.exports = function(id, imageURL, name, artist, tracks, trackStarts) {
	this.id = id;
	this.imageURL = imageURL;
	this.name = name;
	this.artist = artist;
	this.tracks = tracks;
	this.trackStarts = trackStarts;
};